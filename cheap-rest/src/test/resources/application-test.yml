# Test configuration - uses embedded PostgreSQL
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never

cheap:
  database:
    type: postgres
  pagination:
    default-page-size: 10
    max-page-size: 50
