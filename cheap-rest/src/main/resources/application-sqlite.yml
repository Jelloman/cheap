# SQLite configuration
spring:
  datasource:
    url: jdbc:sqlite:${CHEAP_DB_PATH:./cheap.db}
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: never

cheap:
  database:
    type: sqlite
